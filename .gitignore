# OpenTofu — state stays local on purpose (ADR-0001), secrets live in untracked tfvars.
# All tfvars variants are ignored (only the .example templates are tracked) so a stray
# `git add` can never commit a real token.
*.tfstate
*.tfstate.*
*.tfvars
*.auto.tfvars
*.tfvars.json
!*.tfvars.example
.terraform/
.terraform.lock.hcl.tmp

# Build outputs
apps/backend/target/
apps/frontend/dist/
apps/frontend/node_modules/
node_modules/

# Local scratch (issue tracker lives here on purpose — tracked; tmp/ is not)
tmp/

# IDE / OS noise
.idea/
.vscode/
*.iml
.DS_Store

# Session log (per user convention: never committed)
SESSION.md

# Agent worktrees (transient build isolation)
.claude/worktrees/
