# Database Observability explain plans fail — `db-o11y` cannot read the app tables

Status: resolved

Clicking explain plan returns "Explain plan not available due to unrecoverable database
error. This query will not be retried." PostgreSQL requires the same privileges to
`EXPLAIN` a query as to execute it, and the collector's role holds none on our tables.

## Evidence

Reproduced directly against the live database:

    kubectl exec -n shop shop-db-1 -c postgres -- psql -d shop \
      -c 'SET ROLE "db-o11y"; EXPLAIN (FORMAT JSON) SELECT * FROM products WHERE name ILIKE '%widget%';'
    SET
    ERROR:  permission denied for table products

- `has_table_privilege('db-o11y','public.products','SELECT')` → `f` (schema USAGE → `t`).
- `db-o11y` is a member of `{pg_monitor, pg_read_all_stats}` only —
  `infra/40-platform/cloudnative-pg.tf:80-84`. All six tables are owned by `shop`; no
  grant to `db-o11y` exists anywhere.
- At scale: of 3946 `explain_plan_output` records in Loki over 3h, 2682 were
  `skipped/query denylisted`, 1164 `skipped/query contains reserved word`, and exactly
  **100 succeeded — every one a catalog query**. Of the 57 successful digests resolvable
  in `pg_stat_statements`, **zero** touch a user table.

The rest of the pipeline is healthy: the `database_observability.postgres "shop_db"`
component renders correctly (`k8s-monitoring.tf:121-168`), the singleton Alloy pod is
Running 2/2, `pg_stat_statements` v1.12 is installed with `compute_query_id=on`, and
`pg_stat_statements_*` metrics arrive in Grafana Cloud correctly labelled.

## Fix

Add to `postInitSQL` in `infra/40-platform/cloudnative-pg.tf:80-84`:

    GRANT USAGE ON SCHEMA public TO "db-o11y";
    GRANT SELECT ON ALL TABLES IN SCHEMA public TO "db-o11y";
    ALTER DEFAULT PRIVILEGES IN SCHEMA public GRANT SELECT ON TABLES TO "db-o11y";

The third statement matters because Flyway creates the tables *after* bootstrap.

## Both halves are required — decided at triage

- **`postInitSQL` only runs at cluster bootstrap**, so editing it alone does nothing for
  the currently running database. Do **both**: apply the three statements out-of-band to
  the live database (via `kubectl exec -n shop shop-db-1 -c postgres -- psql -d shop`, as
  the superuser, not as `db-o11y`) *and* add them to `postInitSQL` so fresh sessions
  inherit the grants. Neither half alone is sufficient — the out-of-band apply is what
  makes this session's explain plans work and what the done-criterion below tests; the
  `postInitSQL` edit is what stops the next `start.sh` from regressing it.
- **The privilege expansion is accepted, deliberately.** Granting SELECT to `db-o11y` is
  a real widening of a monitoring role. It is in scope here because the collector never
  issues `EXPLAIN ANALYZE` — it reads planner row estimates only, so SELECT does not let
  it execute application queries. Do not widen further than the three statements above;
  in particular no `GRANT ALL`, no ownership change, and no grants outside `public`.

Done: an explain plan renders in DB O11y for a query against `products`.

## Comments

2026-08-07: Filed from the issue-10 validation session. The failure was reproduced
directly; the "will not be retried" wording matches a cached hard permission error, and
Alloy logs no explain-related error at all (inferred: the collector caches the failure).

> *This was generated by AI during triage.*

2026-08-07 triage: state confirmed as `ready-for-agent` after resolving the two open
questions above, which were the reason to doubt it — an unmade decision and an
unauthorised privilege change are both things an AFK agent should not settle for itself.
Maintainer chose apply-now-plus-`postInitSQL` over either half alone, and accepted the
grant explicitly, so the ticket is now fully specified. Note the collector caches its
hard permission failure, so an Alloy pod restart may be needed before an explain plan
renders — a stale "not available" panel is not evidence the grants failed.

2026-08-07: Resolved (commit `7f0f610`, branch `worktree-issues-20-23`), with **two
deviations from this ticket's literal fix**, both doc-verified in a pre-approval security
review because the fix as written above is a silent no-op:

- The three statements went into **`postInitApplicationSQL`**, not `postInitSQL`: CNPG
  runs `postInitSQL` connected to the `postgres` maintenance database, where all three
  statements succeed without ever touching the `shop` schema
  (cloudnative-pg.io/docs bootstrap: "The application database (`postInitApplication`
  section)").
- The third statement gained **`FOR ROLE "shop"`**: default privileges attach to the
  creating role, and Flyway creates the tables as `shop` — un-roled, the entry would
  cover only superuser-created objects (postgresql.org ALTER DEFAULT PRIVILEGES:
  "objects created by the target_role, or the current role if unspecified").

Out-of-band apply audit (grants are invisible to tofu — this comment is the record):
2026-08-07T13:47:07Z UTC, as superuser via
`kubectl exec -n shop shop-db-1 -c postgres -- psql -d shop`:

    GRANT USAGE ON SCHEMA public TO "db-o11y"
    GRANT SELECT ON ALL TABLES IN SCHEMA public TO "db-o11y"
    ALTER DEFAULT PRIVILEGES FOR ROLE "shop" IN SCHEMA public GRANT SELECT ON TABLES TO "db-o11y"

The alloy-singleton collector was rollout-restarted at ≈13:48Z to purge the cached
permission failure. A server-side dry-run confirmed the CNPG webhook accepts the
`spec.bootstrap` patch on the live cluster (ops note: any manual patch to this CR must
use `--server-side`; client-side apply would write the interpolated password into the
last-applied annotation).

Verified live by a fresh-context verifier, 13:56–14:00Z: 69 `explain_plan_output`
successes in Loki (13:55–13:57Z) whose digests resolve in `pg_stat_statements` to
`order_items`, `orders` and `reviews` queries — user tables, against the pre-fix baseline
of exactly zero; zero `permission denied` lines post-restart; `\ddp` shows the roled
entry `"db-o11y"=r/shop`; a `shop`-created probe table inherited SELECT (future-table
proof for Flyway); done-criterion met.

> *This was generated by AI during implementation.*
