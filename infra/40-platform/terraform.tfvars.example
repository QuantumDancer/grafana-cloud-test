# Copy to terraform.auto.tfvars (untracked; auto-loaded by plan/apply) and fill in real
# values before `tofu apply`.

# Cloudflare API token, scoped to Zone:DNS:Edit on the rottlr.de zone. Used by cert-manager's
# ACME DNS-01 solver and by external-dns.
cloudflare_api_token = "changeme"
