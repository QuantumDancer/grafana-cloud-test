{{- if .Values.loadgen.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-loadgen-script
  labels:
    {{- include "shop.labels" . | nindent 4 }}
    app.kubernetes.io/component: loadgen
data:
  shop-load.js: |
{{ .Values.loadgen.script | indent 4 }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-loadgen
  labels:
    {{- include "shop.labels" . | nindent 4 }}
    app.kubernetes.io/component: loadgen
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/component: loadgen
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/component: loadgen
        app.kubernetes.io/instance: {{ .Release.Name }}
      annotations:
        # Forces a rollout when only the mounted script content changes —
        # ConfigMap volume updates otherwise propagate to the pod's
        # filesystem eventually (kubelet sync period) but k6 itself never
        # notices/reloads a running script anyway.
        checksum/script: {{ .Values.loadgen.script | sha256sum }}
    spec:
      # k6's own process exit (whenever the script's scenario finishes its
      # bounded duration — see shop-load.js's comment on why it isn't
      # literally infinite) is exactly what should trigger the next run;
      # the Deployment's implicit restartPolicy: Always on the Pod template
      # is what gives "continuous" load without k6 needing to loop
      # internally or this chart needing a CronJob.
      #
      # CONSEQUENCE, and it looks alarming on purpose-built dashboards: this
      # container's restartCount climbs forever, roughly once an hour, for as
      # long as the stack is up. That is success, not a crash loop. A healthy
      # cycle terminates with exitCode 0 / reason "Completed" and the next
      # container starts ~1s later, with no BackOff event; kubelet's
      # exponential restart backoff never engages because a run lasts far
      # longer than the interval at which that backoff resets. Before
      # investigating a "restart-looping loadgen", check the exit code —
      # anything non-zero is real (k6 exits 99 on a threshold breach, and an
      # OOM kill would show reason "OOMKilled"). The cadence was 14 minutes
      # (~100 restarts/day) until 2026-08-07, which was mistaken for a crash
      # loop twice; it is now 55 minutes, matching the browser loop.
      containers:
        - name: k6
          image: {{ .Values.loadgen.image }}
          imagePullPolicy: {{ .Values.loadgen.pullPolicy }}
          args:
            - run
            - /scripts/shop-load.js
          env:
            # Empty by default: shop-load.js falls back to the in-cluster
            # backend Service DNS name itself when unset, so this only needs
            # overriding for a one-off pointed elsewhere.
            - name: BASE_URL
              value: {{ .Values.loadgen.baseUrl | default (printf "http://%s-backend:%v" .Release.Name .Values.backend.service.port) | quote }}
          volumeMounts:
            - name: script
              mountPath: /scripts
              readOnly: true
          resources:
            {{- toYaml .Values.loadgen.resources | nindent 12 }}
      volumes:
        - name: script
          configMap:
            name: {{ .Release.Name }}-loadgen-script
{{- end }}
